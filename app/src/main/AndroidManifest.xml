<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="appteam.nith.hillffair2k18">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.DashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RouletteActivity"></activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.Housie"></activity>
        <activity android:name=".activity.Gallery" />
        <activity android:name=".activity.Profile" />
        <activity android:name=".activity.ImageUI" />
        <activity android:name=".activity.Upload" />
        <activity android:name=".activity.AppTeam"></activity>
        <activity android:name=".activity.SettingsActivity"></activity>
    </application>

</manifest>